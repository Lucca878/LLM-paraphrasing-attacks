name: llm-attacks
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    - openai
    - python-dotenv
    - transformers
    - torch
    - pandas
    - numpy
    - safetensors
